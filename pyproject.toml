[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "arity-pre-commit"
version = "0.22.0"
description = "pre-commit hook wrapper for arity"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = ["arity==0.22.0"]

[tool.setuptools]
py-modules = []
