[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "arity-pre-commit"
version = "0.19.1"
description = "pre-commit hook wrapper for arity"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = ["arity==0.19.1"]

[tool.setuptools]
py-modules = []
